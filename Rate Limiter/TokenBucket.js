// Define a Bucket object
// Define a Token object
// Implement bucket refil strategy after each cycle
// Request wrapper, this will receive the request and thent take it through the rate limiting mechanism

function tokenBucketMiddleware(req, res, next) {}
